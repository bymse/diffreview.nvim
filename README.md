# diff-review.nvim

## Tooling

Run `just bootstrap` to download the pinned Neovim AppImage. 
StyLua with LuaJIT support and Lua Language Server 3.18.2 or newer must be available as `stylua` and `lua-language-server` on `PATH`.
