# Configuration

## Diff view

- `diff.layout`: Controls the two-window layout for text diffs. Supported values are `horizontal`, which places the old buffer above the current buffer, and `vertical`, which places the old buffer to the left of the current buffer. The default is `horizontal`.
